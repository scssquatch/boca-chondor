= form_for @name, remote: true do |f|
  p What's the Name?
  = link_to "GENERATE A NAME", generate_path, remote: true, class: "generate_name"
  .errors
  = f.text_field :name
.names
  = render @names
